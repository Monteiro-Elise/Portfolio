#!/usr/bin/env sh
set -e

echo "Running lint-staged (ESLint + Prettier)..."
npx --no-install lint-staged

echo "Running TypeScript check..."
npx --no-install tsc --build --noEmit

echo "All pre-commit checks passed!"
exit 0
